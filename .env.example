PORT=4000
PROXY_KEY=your-proxy-secret-key
REQUEST_TIMEOUT=60000
LOG_LEVEL=info

# WARP SOCKS5 代理（可选，用于 IP 混淆）
# Docker 部署时使用 host.docker.internal 访问宿主机的 WARP 代理
# 使用 socks5h:// 确保 DNS 也走代理
# WARP_PROXY=socks5h://host.docker.internal:40001
